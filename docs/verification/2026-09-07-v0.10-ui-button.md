# v0.10 / 10-05a Button 本地验收

## 状态与基线

10-05a 已完成本地实现和验证；分支为 `codex/v0.10-ui-buttons`，基于 `codex/v0.10-ui-text`，沿用包含 `c71ad50` main 与 10-04a 的基线，保留上一包 10-04b 本地改动。Text 包已提交 PR #85，面向 main 并携带 #84 布局依赖；Button 包单独基于 Text 分支评审。两包尚未合并或发布。下一包为 10-05b 固定战斗、胜负/重开和结构化游戏快照，整个 v0.10 尚未完成。

实现边界见 [Button 设计](../superpowers/specs/2026-09-07-v0.10-ui-button-design.md)。不新增依赖，不修改默认 Battle 场景，不引入 MCP 输入注入。

## 自动验证

命令在 Visual Studio Developer PowerShell、MSVC 14.38 x64 环境执行。

| 命令 | 结果 |
| --- | --- |
| `cmake --preset windows-msvc-debug` | 配置成功 |
| `cmake --build --preset windows-msvc-debug --parallel 2` | 构建成功，最终产物已移除临时原生键盘诊断 |
| `cmake --preset windows-msvc-debug-tests` | 配置成功 |
| `cmake --build --preset windows-msvc-debug-tests --parallel 2` | 全量构建成功 |
| `out/build/windows-msvc-debug-tests/Tests/JanusTests.exe "[button]"` | 17 cases / 228 assertions 全通过 |
| `out/build/windows-msvc-debug-tests/Tests/JanusTests.exe "Managed Application and Editor runtime agree on every input frame and timestep"` | 1 case / 49 assertions 全通过 |
| `ctest --preset windows-msvc-debug-tests` | **351/351，0 failures，15.41 秒** |
| `git diff --check` | 通过，无空白错误 |

完整 configure、build、CTest 输出已逐段读取。日志保留在忽略目录 `out/`：`v010-button-configure-debug.log`、`v010-button-configure-tests-final.log`、`v010-button-build-debug-final.log`、`v010-button-build-debug-complete.log`、`v010-button-build-tests-final.log`、`v010-button-build-tests-complete.log`、`v010-button-host-parity.log`、`v010-button-narrow-complete.log`、`v010-button-ctest-complete.log`。

覆盖：重叠最上层/子标签/无关图元遮挡、同帧短按、拖出后返回、隐藏/删除/失焦、按住跨 Game View 边界、面板起源按压过滤、双向焦点循环/跳过禁用、Enter/Space 单次确认、输入消费/释放连续性、256 条记录溢出保护、Start/Resume 不重放、反射保存/Clone、Human Undo/颜色校验/Runtime guard、渲染瞬时颜色、OnClick→Update 顺序/错误/陈旧实体、双 Runtime 计数/中性 Step。Modern/Legacy 外部 stdio 测试均覆盖 Button 字段修改和事务回滚。

初轮失败已解决：测试夹具兄弟插入顺序、反射/资产数量断言，以及宿主一致性测试的物理像素预期。独立窗口现在向脚本提供项目逻辑坐标，800 像素测试窗口中的 x=2 对应 1280 逻辑宽度中的 x=3.2；双入口每帧及最终值均按该合同核验。

## 真实窗口验证

使用 computer-use 技能，在忽略目录 `out/v010-button-demo` 复制项目并将 defaultScene 指向 `Scenes/ButtonShowcase.scene`。原始 `SandboxProject/project.json` 保持 Battle。

- `JanusSandbox.exe <项目副本>`：加载 7 个实体；点击 A 得到 1，Enter 得到 2；拖出后仍为 2；点击 B 得到 1，Space 得到 2。按钮焦点和禁用颜色可见。正常关闭，进程返回 0。
- `JanusEditor.exe --project <项目副本>`：Game View 预览两个 0；Play 后点击 A 得到 1，Enter 得到 2；Pause/Step 保持 2；Stop 后回到作者态两个 0。正常关闭，进程返回 0。
- Up/Down 的真实键盘遍历未由自动注入完成：诊断观察到工具将 `Down` 发为 SDL scan code 90（`SDL_SCANCODE_KP_2`），而 `SDL_SCANCODE_DOWN` 是 81。循环遍历、反向循环和确认由自动测试验证；未将本次注入视作方向键原生通过。临时 SDL 日志代码已按字节恢复，平台文件无 Git diff，正式 Debug 产物已重新构建。

原生日志为 `out/v010-button-sandbox.log`、`out/v010-button-editor.log`；输入诊断为 `out/v010-button-native-probe.log`。保留既有 OpenGL 131218 性能提示；配置中的可选 PkgConfig/LibUSB 缺失、既有 getenv C4996 和测试 nodiscard C4834 不阻止构建。新增代码的局部变量遮蔽告警已消除。

## 已知范围

首版仅左键点击、Up/Down 导航、Enter/Space 确认、单实体固定 OnClick；没有可配置导航映射、跨实体事件表达式、手柄、复杂布局或战斗规则。输入消费以本帧 key/button 为粒度；Panel/Image/Text 不自行捕获。字体能力仍遵循 10-04b 的离线图集与字形集合。日志、临时项目、构建产物均在 Git 忽略目录。

## PR 拆分后复核

在独立分支 `codex/v0.10-ui-buttons`（基于 Text 提交 `9c53e64`）再次执行两套预设的 configure/build，全部成功；`ctest --preset windows-msvc-debug-tests` 为 **351/351，0 failures，13.21 秒**。完整输出已读取，`git diff --check` 通过。日志位于忽略目录 `out/pr-button-configure-debug.log`、`out/pr-button-configure-tests.log`、`out/pr-button-build-debug.log`、`out/pr-button-build-tests.log`、`out/pr-button-ctest.log`。
